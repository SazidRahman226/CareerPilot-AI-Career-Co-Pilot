# Set a memory limit (e.g., 2 Gigabytes) so Redis doesn't crash your server
maxmemory 2gb

# When memory is full, automatically delete the least recently used keys with a TTL
maxmemory-policy allkeys-lru

# Enable Append Only File (AOF) persistence so you don't lose cached data on restarts
appendonly yes
appendfsync everysec
